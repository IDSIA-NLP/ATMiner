# Notes

# Entity Recognition

The `EntityRecognizer` should take a text string and retuern the a list of dicts with:
```python
[{
    'entity_group': 'PER', 
    'score': 0.9988506, 
    'word': 'Sylvain', 
    'start': 11, 
    'end': 18,
    'length':
    ''
}]
```